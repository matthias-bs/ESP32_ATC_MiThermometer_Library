sudo: false
language: ruby
dist: bionic
git:
  depth: false
  quiet: true
# Blacklist
branches:
  except:
    - gh-pages
before_install:
  - sudo apt-get install -y doxygen graphviz
script:
   - bundle install
   - bundle exec arduino_ci_remote.rb
   - doxygen doc/Doxyfile
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 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
  keep-history: false
  local-dir: doc/html
  verbose: true
  on:
    branch: master
